package io.jiffy.stax.plugin.pe;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

public class PePlugin extends Plugin {
    public PePlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
